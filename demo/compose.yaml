# Generated by create-polyglot
version: 3.9
services:
  node:
    build:
      context: ./services/node
    container_name: demo-node
    ports:
      - 3001:3001
    environment:
      PORT: 3001
    networks:
      - app-net
networks:
  app-net:
    driver: bridge
